[project]
name = "quant"
version = "0.1.0"
description = "Stock valuation analysis using quantitative metrics"
requires-python = ">=3.12"
dependencies = [
    "matplotlib",
    "python-dotenv",
    "requests",
    "yfinance",
]

[dependency-groups]
dev = [
    "pytest",
]
